mezicash.seguranca.enable-http=true

spring.datasource.url={JDBC_DATABASE_URL}
spring.datasource.username={JDBC_DATABASE_USERNAME}
spring.datasource.password={JDBC_DATABASE_PASSWORD}

mezicash.origin-permitida=https://mezicash-angular.herokuapp.com

#Criar as tabelas automaticamente
#spring.jpa.generate-ddl=true

#Reseta banco a cada inicialização -> dev
#spring.jpa.hibernate.ddl-auto=create

#Exibir logs de consulta
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#Falhar requisição ao receber atributos desconhecidos (por segurança)
spring.jackson.deserialization.fail-on-unknown-properties=true